#!/bin/bash
# DIYkit package: The Python programming language

        NAME=python
     VERSION=2.7
    DISTFILE=Python-${VERSION}.tar.bz2
 MASTER_SITE=http://www.python.org/ftp/${NAME}/${VERSION}
 ARCHIVE_URL=${MASTER_SITE}/${DISTFILE}

. $DIYkit/DIYkit-include.sh

CHECK_AVAILABLE "python -c 'import sys; exit(0 if (sys.version>=\"2.6\") else 1)'"
DOWNLOAD_ARCHIVE
UNPACK_ARCHIVE

XXXXXXXXXX-unfinished-XXXXXXXXXX

cd $SRCDIR
#./configure --prefix $PREFIX
#make
#make install
