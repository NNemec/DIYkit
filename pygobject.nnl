        NAME=pygobject
     VERSION=2.16.1
    DISTFILE=${NAME}-${VERSION}.tar.bz2
 MASTER_SITE=http://ftp.gnome.org/pub/GNOME/sources/pygobject/2.16
 ARCHIVE_URL=${MASTER_SITE}/${DISTFILE}
   AVAILABLE="pkg-config --exists pygobject-2.0"

. $NNlab_path/NNlab-include.sh

DOWNLOAD_ARCHIVE
UNPACK_ARCHIVE
# PATCH_ARCHIVE

cd $SRCDIR/$NAME
# CPPFLAGS="-I/usr/include/glib-2.0 -I/usr/lib/glib-2.0/include" 
./configure --prefix $PREFIX
make
make install
