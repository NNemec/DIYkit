#!/bin/bash

. ~/NNlab/NNlab.sh

(
cd $NNlab_path

if [ $# -eq 0 ] ; then
    if [ "`basename -- $0`" == "NNlab" ] ; then
        exec bash
    fi
else

    CMD=$1
    shift

    case $CMD in
    install)
        bash ./$1.sh
        ;;
    depend)
        DEPENDENCY_ONLY=true bash ./$1.sh
        ;;
    avail)
        ( for x in *.sh ; do basename $x .sh ; done ) | grep -v NNlab | sort
        ;;
    update)
        git pull
        ;;
    *)
        exec $CMD "$*"
        ;;
    esac
fi
)
