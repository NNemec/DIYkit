#!/bin/bash
# DIY package: ???

        NAME=bibus
     VERSION=1.3.0
    DISTFILE=${NAME}-${VERSION}.zip
 MASTER_SITE=http://download.sourceforge.net/project/${NAME}/${NAME}/${VERSION}/bibus-biblio
 ARCHIVE_URL=${MASTER_SITE}/${DISTFILE}

. $DIYkit/DIYkit-include.sh

DOWNLOAD_ARCHIVE
UNPACK_ARCHIVE

cd $SRCDIR

XXX python=???

make DESTDIR=$PREFIX python=$PREFIX/bin/python sysconfdir=$PREFIX/etc install
ln -sf ../share/bibus/bibusStart.py $PREFIX/bin/bibus
