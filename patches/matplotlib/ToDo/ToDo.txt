* deactivate autoscaling after xlim or ylim were set
  (take care of logscale -> warn if negative lims but run on with autoscale)
  
* reduce number of ticks in low resolution

* allow color=.5 in axvline/axhline

* make exponents in ticklabels into tspan

* allow None for ticklabels in set_ticks
